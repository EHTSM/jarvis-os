# MASTER OPEN FINDINGS — Authoritative Table

Date: 2026-08-15 · Branch: `security/reality-completion`

**One authoritative table.** No contradictory status duplicated across other reports — where another report narrates a finding, this table is the source of truth for its final disposition.

Every ID traces back to `reports/C10-FINAL-CLOSURE-INVENTORY.md`.

---

| ID | Source | Severity | Finding | Root Cause | Action Taken | Status | Evidence | Regression | Final Disposition |
|---|---|---|---|---|---|---|---|---|---|
| C10-001 | C.10 | P0 | `/dev/*` (36 routes) unauthenticated | Missing `requireAuth` | Fixed in C.10 | Closed | Live-verified pre-Master-Recovery | 181/181 at the time | ALREADY FIXED |
| C10-002 | C.10 | P0 | `/cbeta/billing/*` cross-account IDOR | Missing ownership check | Fixed in C.10 | Closed | Live-verified pre-Master-Recovery | 181/181 at the time | ALREADY FIXED |
| C10-003 | C.10 | P1 | Developer OS zero org scoping | `developerOS.cjs` had no `orgId` concept anywhere | **FIXED this phase** — full rewrite, `orgId` required on every data-access function, route layer threads `req.org.id`, legacy unowned records correctly invisible (not misattributed), one-time migration function provided but not auto-run | Closed | Two real tenants, direct-ID, search, stats, dashboard all re-tested; persistence across restart confirmed | 184/184 → 190/190 | FIXED |
| C10-004 | C.10 | P1 | Memory OS (missionMemory.cjs) zero org scoping, AI mission-context cross-tenant leak (also C.9 finding) | `missionMemory.cjs`'s `listMissions()` had no org filter; `codingAssistant.js`'s `_missionContext()` called it unscoped | **FIXED this phase**, scope-corrected from the original item: `orgId` made OPTIONAL (not required) on `createMission`/`listMissions` to avoid breaking 74 internal non-tenant consumers; the actual tenant-facing leak point (`_missionContext()`) now threads the real caller `orgId` through via newly-mounted `attachOrg` on `/coding/*` | Closed | Real cross-tenant marker test: Org B query empty, Org A query correct, unscoped legacy query unchanged for the 74 internal callers | 184/184 → 190/190 | FIXED |
| C10-005 | C.10 | P2 | 3 non-reconciled Memory OS backends (`/memory/*`, `/memory-index/*`, `/p18/memory/*`) | Historical accretion — 3 separate systems built at different times, never unified | **Not fixed** — investigated: `/memory-index/*` (`unifiedMemoryEngine.cjs`) is explicitly commented "previously built but unwired" in its own route mount; `/p18/memory/*` is the legacy system actually used by `MemoryOSV2.jsx`; `/memory/*` (`engineeringMemoryEngine.cjs`) is a read-only aggregator over 14 further engineering-source engines (13 of which also have zero org scoping — see below) | Open | Route-mount comments confirm the "unwired" characterization is still accurate | N/A | VERIFY — requires a product decision on which backend is canonical before any consolidation or scoping work makes sense; consolidating 3 systems is out of a recovery phase's minimal-fix mandate |
| C10-004b (new, surfaced during recovery) | Master Recovery | P1/P2 | 13 of 14 engineering-memory source engines (rule registry, RCA engine, pipeline coordinator, decision engine, smell detector, confidence engine, viz engine, etc.) have zero `orgId` | Same architectural pattern as missionMemory, at 13x the surface area | **Not fixed** — most of these are legitimately platform-wide engineering intelligence (a coding rule or root-cause pattern is not naturally "owned" by one tenant the way a mission or a repo is), so blanket org-scoping would repeat exactly the mistake C10-004's fix specifically avoided. Escalated, not silently patched | Open | `grep -c orgId` = 0 across 13 files, individually confirmed | N/A | VERIFY — genuinely requires a product decision, engine-by-engine, on which of the 13 hold tenant-owned data (none confirmed to leak actual tenant business data in this session's testing — they hold engineering-process intelligence, not customer/business records) vs. which are correctly platform-wide |
| C10-006 | C.10 | P2 | No unified Finance OS; Executive OS (`/eos/v6/*`) is platform-wide operator-only, can't reflect one org's real revenue | Two intentionally-separate systems (platform SaaS billing vs. per-org sales revenue) that the mission's own Flow 1 template assumed were one | Not fixed — investigated: `/org-executive/:orgId/*` exists, is genuinely org-scoped, and was not live-tested this session as the possible intended completion of this flow | Open | `/business/dashboard` (real, tested, correct) already serves the practical need; `/eos/v6/*` correctly 403's a non-operator | N/A | VERIFY — needs a product decision on whether `/org-executive/:orgId/*` should be surfaced as "Executive OS" for regular org owners, which is a scope/architecture decision, not a bug fix |
| C10-007 | C.10 | P1→P2 (reclassified, then partially closed) | Automation OS has no live execution loop — rules created/dry-run only | A concurrent pass had already reconciled this as: `manual` (fire route) and `schedule` (real `node-cron` dispatch in `orgAutomationScheduler.cjs`, org-scoped, boot-started) genuinely worked; only `event`/`threshold`/`webhook`/`approval` had zero dispatcher. **This phase closed the `event` type**, the one of the 4 with an existing, reusable primitive to build on (`runtimeEventBus.subscribe()`, already used platform-wide) — added `automationService.startEventLoop()`, one process-wide subscription that scans every workspace's enabled `event`-type rules on every bus event and calls the existing `fireRule()`; wired at server boot right after the bus itself starts. Coexists with `orgAutomationScheduler.cjs`'s cron dispatcher with no shared state or route conflict (different route prefixes — `/automation/*` workspace-scoped vs `/org-automation/:orgId/*` org-scoped — both correctly call into the same single `automationService.fireRule()`, not two engines). While building this, found and fixed a genuine, previously-unknown concurrency bug: `fireRule()`'s read-modify-write of `data/automation-layer.json` was not reentrancy-safe — an `emit_event` action synchronously triggering another rule's `fireRule()` mid-execution caused the outer call's stale in-memory snapshot to silently overwrite (clobber) the inner call's history/runCount write on save. Fixed by serializing all `fireRule()` calls onto a single promise chain. `threshold`/`webhook`/`approval` remain correctly unbuilt — each requires a real product decision (a metrics-threshold monitor, an inbound-webhook-to-tenant-identity mapping, and approval-flow semantics respectively) this pass has no authority to invent unilaterally | Closed (event type fixed; threshold/webhook/approval remain a named, explicit gap) | Live end-to-end: real event-type rule created, a second rule's manual fire synchronously emitted the matching event, the event-type rule's history/runCount updated automatically with no manual trigger — reproduced the clobber bug first (rule silently never appeared in history despite the event firing), then reproduced the fix (rule fires and persists correctly every time). Negative-tested: reverted the serialization fix, confirmed the regression test failed with the expected message, restored | 208/208 → 211/211 | **event type: FIXED. threshold/webhook/approval: FOUNDER/PRODUCT DECISION REQUIRED** — genuinely requires product decisions on trigger semantics; not built, not silently left as "open" |
| C10-008 | C.10 | P2 | No `deleteRule`, no dedicated resume/trigger route | Same root cause as C10-007 | **FIXED this phase** — `automationService.deleteRule()` added (removes a rule, audit-logged, emits `automation:rule:deleted`); `DELETE /automation/rules/:id` and `POST /automation/rules/:id/fire` routes added (the latter also used as the live-verification harness for C10-007's event loop, since it lets any rule — including an `emit_event` action — be triggered on demand through the exact same `fireRule()` path the automatic dispatchers use) | Closed | Live-verified: created rule → disabled it → confirmed a matching event does NOT fire it (runCount stays put) → deleted it → confirmed it no longer appears in the rules list | 208/208 → 211/211 | FIXED |
| C10-009 | C.10 | P1 | Knowledge OS frontend (`KnowledgeCenter.jsx`) 100% fabricated seed data, 3 real backends unused | Frontend built as a demo/mock and never connected | **BUILT this phase** — `KnowledgeCenter.jsx` fully rewritten (all SEED_DOCS/SEED_WEBSITES/SEARCH_RESULTS fabrication removed) to fetch real org context via `/orgs/me/context`, then real graph data via the existing org-scoped `orgKnowledgeGraph.js` (`GET /org-graph/:orgId`, `POST /org-graph/:orgId/index`, `GET /org-graph/:orgId/impact/:type/:id`) — no new backend created, per the mission's explicit instruction not to build a second Knowledge backend. Honest empty/loading/error states; real per-type node counts; real impact/dependency panel on node selection | Closed | Live two-tenant test: Org A indexed real leads/opportunities/campaigns/connectors/automation rules, Org B's graph independently empty until its own index; impact panel shows real `BELONGS_TO` edges, no fabricated content anywhere in the component (verified by regex-negative tests) | 192/192 → 195/195 | FIXED |
| C10-010 | C.10 | **P0 (escalated this phase — was P2)** | Enterprise OS: 3 non-integrated backends, UI uses the least-discoverable one with its own independent membership model | Historical accretion, same class as C10-005 — turned out to be far more severe than the original P2 framing: the legacy `enterpriseOS.cjs` engine (32 routes in `ops.js`) had **zero authentication middleware of any kind**, not merely "an independent membership model" | **FIXED this phase (25-OS Master Reconciliation, dedicated Enterprise OS pass).** Live-reproduced on an isolated port: a bare, cookie-less HTTP request could list every org on the platform, create/rename/archive any org (a real pre-existing seeded org, `Acme Global`, was renamed to `HACKED-ACME-BY-NOAUTH` and archived with zero auth header sent, then fully restored), create depts/roles/teams under any org ID, and read the platform-wide audit log. Root cause: `ops.js` registers these 32 routes with no preceding `router.use(..., requireAuth, ...)`, and nothing upstream in `index.js`'s middleware chain catches them either. Fixed with `_eosGate = [requireAuth, operatorOnly, operatorAudit]` applied per-route (not a prefix gate — a first attempt via `router.use(prefix,...)` was caught mid-verification shadowing the real, already-safe `/enterprise/dashboard/:orgId` and `/enterprise/audit/:orgId/search` routes from unrelated M1-M8 modules, corrected before considering the fix complete) | Closed | Independently re-verified live against the actual port-5050 server (not just the isolated test port): unauthenticated `GET /enterprise/orgs` → 401. Full reproduction/fix/regression evidence in `reports/OS-ENTERPRISE-FINAL.md`; 4-question disposition in `reports/OS-ENTERPRISE-RECONCILIATION.md` | 211/211 before and after; extended `tests/security/97-enterprise-isolation-integrity.cjs` with 2 new sections (8/8), negative-tested for real (reverted, confirmed exact expected failure, restored) | **FIXED** — the architectural question (should the 2 membership models eventually consolidate?) remains **FOUNDER DECISION**, correctly not resolved here, but the P0 security exposure is fully closed |
| C10-011 | C.10 | P3 | Platform OS backend real, zero frontend consumer | `OrgLevelStatus.jsx`'s levels map omits Platform | Not fixed — genuinely dead capability, no security/tenant impact | Open | Confirmed via grep across `frontend/src` | N/A | DEFERRED |
| C10-012 | C.10 | P2 | Support OS frontend (`SupportCenter.jsx`) doesn't call the real, hardened `/customer-org/support/*` backend | Frontend built with honest sample-data fallback, never wired to the real backend | **BUILT this phase** — `SupportCenter.jsx` fully rewritten (all `SEED_TICKETS`/`KB_ARTICLES`/localStorage persistence removed) to fetch real org context via `/orgs/me/context`, then real tickets via `GET /customer-org/support/tickets`, `GET .../ticket/:id`, resolve via `POST .../ticket/:id/resolve`. The real ticket shape (`{id, customerId, orgId, issue, category, severity, status, stage, health, suggestedResolution, createdAt, updatedAt}`) differs materially from the old fake shape (no `subject`/`priority`/`waitHours`/`assignee`/`tags`/SLA-hours on any real record) — fields not present on the real record are not fabricated. `GET .../support/stats` confirmed itself platform-wide (not org-scoped) via source read — summary tiles computed client-side from the already org-scoped ticket list instead, to avoid introducing a leak via the frontend. KB/SLA-policy/analytics tabs removed — no real KB-article or SLA-policy backend exists to connect to | Closed | Live two-tenant test with a real secret-labeled ticket: Org B's list did not contain it, direct-ID read 404'd, cross-tenant resolve attempt 404'd (not 200), Org A's own resolve succeeded and persisted (`status:"resolved"`, real `resolvedAt`) | 205/205 → 211/211 | FIXED |
| C10-013 | GAP-LIST #4 (orig.) | P0 (orig.) | No RBAC/permission-assignment step in company factory | Original finding's own comment ("no parallel company-scoped storage or permission system is created here") describes REUSING the real `organizationService.createOrg()` RBAC system, not lacking one | **Investigated, found ALREADY FIXED** — `organizationService.createOrg()` genuinely assigns the creator as `org_owner` with full real RBAC on every company creation; `companyFactory.cjs`'s own code comment at the department-creation step explicitly confirms "real org_owner role, assigned at org creation... passes the same real permission check any operator action would" | Closed | Static trace of `createOrg()` (creates `members:[{accountId, orgRole:"org_owner"}]`) + `companyFactory.cjs`'s own confirming comment; a live end-to-end test was attempted but a standalone script inadvertently triggered the real autonomous engineering/business simulation loops as a side effect of requiring `companyLifecycleEngine.cjs` and was aborted rather than risk a longer-running side effect on shared data | N/A | ALREADY FIXED — the original GAP-LIST entry is outdated against current code |
| C10-014 | GAP-LIST #5 | P0 (orig.) | Single-process architecture (`instances:1`), non-cluster-safe | Explicit architectural choice, documented in `ecosystem.config.cjs`'s own comment | Not fixed — this is an infrastructure/deployment decision (moving to a multi-process/clustered architecture with a real shared session/cache store), not a code bug | N/A | N/A | OUT OF SCOPE — architectural decision, correctly acknowledged in-code as intentional for now |
| C10-015 | GAP-LIST #7 | P1 (orig.) | No dynamic skill-creation pipeline | Not independently re-verified this phase | Not investigated this session — genuinely large scope (would require an `eval`/`new Function` sandboxing decision, a real security-sensitive architecture choice) | Open | Prior doc only | N/A | DEFERRED — requires dedicated scope and a security-architecture decision, not a recovery-session fix |
| C10-016 | GAP-LIST #10 | P1 (orig.) | Limited connector coverage (no Salesforce/HubSpot/Zendesk/QuickBooks/Shippo) | Genuinely absent — no code exists | Not built — each is a real external-provider integration requiring real credentials to build/test against, explicitly forbidden this phase | Open | Confirmed absent, prior doc | N/A | CREDENTIAL BLOCKED — cannot be meaningfully built or tested without real provider credentials, deferred to the post-recovery credential-provisioning phase |
| C10-017 | GAP-LIST #12 | P0/P1 (escalated) | `businessDataService.cjs` opt-in-only `orgId` scoping — full call-site audit completed this phase | Legacy pre-multi-tenancy records + call sites that can omit `orgId` | **FIXED this phase.** Full audit of all ~45 `business.js` routes confirmed every direct call site already correctly passes `req.org.id` (Flow 1's chain re-confirmed a 3rd time) — the opt-in design itself is intentional/safe (documented in the service's own header) and was not the live vulnerability. The audit then swept the OTHER 10 consumers of `businessDataService.cjs` and found a genuine, previously-undocumented **live cross-tenant leak** in `unifiedIntelligenceLayer.cjs`: `GET /intelligence/unified/executive` (+`correlate`,`events`,`recommendations`,`score`) called `_readBizState()`/`getHealthMetrics()` with no `orgId` at all, returning one real tenant's leads/revenue/pipeline/KPIs to any other caller. Root cause required TWO fix layers: (1) thread `orgId` through `_readBizState`, `correlate`, `scoreImpact`, `getExecutiveDashboard`, `detectCrossDomainEvents`, `getUnifiedRecommendations`, AND the separate `_bie().getHealthMetrics({orgId})` call that `bizKPIs` actually reads from (first-pass fix missed this second call and the leak persisted until re-tested live); (2) mount `attachOrg` on `/intelligence/unified/*` routes to resolve `req.org` from the caller — but live-testing a forged `X-Org-Id` header then proved `attachOrg` alone (non-blocking by design) does not verify real membership, so `requireOrgMember` was added to the chain too, closing the forged-header bypass | Closed | Live two-tenant test, real data (not empty-vs-empty): Org B initially received Org A's real `activeLeads`/`revenueThisMonth`/`pipelineValue` via `/intelligence/unified/executive`; after fix, Org B's own (different, correct) data returned; forged `X-Org-Id: <Org A id>` from Org B's own token correctly 403s post-`requireOrgMember`; legitimate same-org access re-confirmed for both tenants on all 5 routes | 195/195 → 199/199 | FIXED |
| C10-017b (new, surfaced during this phase's C10-017 audit) | Master Residual Closure | P2 | `businessEventAdapter.cjs`'s external-ingestion event log (`getEventLog()`) has no `orgId` parameter in its signature at all — not a missing pass-through, a missing concept | External webhook/form/email/etc. ingestion pipeline was built before multi-tenancy and has no tenant-identity concept for inbound events | **Not fixed this phase** — this is architecturally different from every other C10-017-class leak fixed this session: those all had a real `orgId` available at the call site that simply wasn't threaded through; here, external events (webhook/email/form submissions) arrive with no tenant context to begin with, so there is no `orgId` to thread without first deciding how inbound external events establish tenant identity (API key → org mapping? per-org webhook URLs? something else?) — a product/architecture decision, not a code fix, and building it unilaterally risks inventing a second tenant-identity model alongside the real one | Open | `grep -n "orgId" backend/services/businessEventAdapter.cjs` = 0 matches in `getEventLog`'s signature or body; confirmed via direct source read | N/A | VERIFY — needs a product decision on how external/webhook ingestion establishes tenant identity before this can be fixed; the read-side leaks this session found and fixed (C10-017 proper) are unrelated and fully closed |
| C10-018 | GAP-LIST #14 | P1 (orig.) | Niche-classification regex ladder, defaults to `saas` | Not investigated this session | Not fixed | Open | Prior doc only | N/A | DEFERRED |
| C10-019 | GAP-LIST #15 | P1 (orig., by design) | Deploy approval defaults off for non-production | Intentional — only `production` profile requires approval | Not fixed — this appears to be deliberate (staging/dev environments reasonably skip approval gates) | N/A | Confirmed via C.10's static read | N/A | OUT OF SCOPE — appears intentional, not a bug |
| C10-020 | GAP-LIST #16-18 | P2 (orig.) | No compliance/legal e-signature, clinical-safety, trading-data infra | Genuinely absent | Not built | Open | Prior doc only | N/A | OUT OF SCOPE for this phase — large, domain-specific builds requiring product scoping decisions |
| C10-021 | GAP-LIST #19 | P2 (orig.) | No geospatial/mapping capability | Genuinely absent, unused API key referenced | Not built | Open | Prior doc only | N/A | OUT OF SCOPE / CONFIG REQUIRED if ever prioritized |
| C10-022 | GAP-LIST #20 | P2 (orig.) | 3D/CAD, Manufacturing/IoT/Robotics/Energy department families label-only | Not investigated this session | Not fixed | Open | Prior doc only | N/A | DEFERRED |
| C10-023 | GAP-LIST #21 | P2 (orig.) | Cross-company intelligence is 2 hardcoded heuristics | Not investigated this session | Not fixed | Open | Prior doc only | N/A | DEFERRED |
| C10-024 | GAP-LIST #22 | P3 (orig.) | "Load-test verified" claims not backed by realistic concurrency | Not investigated this session | Not fixed | Open | Prior doc only | N/A | VERIFY — re-run at realistic concurrency before any public claim, not a code fix |
| C10-025 | GAP-LIST #23 | P3 (orig.) | Duplicate connector-probe code | Not investigated this session | Not fixed | Open | Prior doc only | N/A | DEFERRED — low risk, cosmetic cleanup |
| C10-026 | GAP-LIST #24 | P3 (orig.) | Enterprise CRM frontend (`EnterpriseCRM.jsx`) pure client-side mock | Same class as C10-009 — investigated further this phase (independently, twice — once by the concurrent Audit Track pass, once by this Pre-Credential V1 Perfection pass), found to be a different situation both times | **Investigated, found genuinely orphaned — reclassified, not built (confirmed independently twice).** `grep -rn "EnterpriseCRM" frontend/src/` returns only the file's own two self-references — zero mount points anywhere in `App.jsx` or any other component; a prior audit (`reports/C1.1_DEAD_FRONTEND_REGISTER.md`, `reports/OS-SALES-DISCOVERY.md`) had already independently classified it ARCHIVE/DEAD-ORPHANED before either of this same-day pass's investigations. Two real, already-wired, non-duplicate replacements cover its functional surface: **`ContactsV2.jsx`** (mounted at the "Contacts" nav tab, `tab === "clients"`, calling real `/crm/leads` via `crmApi.js`) for the contacts/leads facet, and **`BusinessOS.jsx`**'s Pipeline view (mounted at the separate "CRM" nav tab, `tab === "business"`, calling real `/business/opportunities/*`) for the stage/value/pipeline facet `EnterpriseCRM.jsx`'s mock was simulating — live two-tenant tested this phase with real secret-labeled opportunities, full tenant isolation confirmed (list-scoped, direct-ID 404, forged-`X-Org-Id` 403, cross-tenant write blocked). Building out `EnterpriseCRM.jsx` would create genuine duplicate CRM architecture — directly against the mission's explicit "never duplicate architecture" constraint | Closed (reclassified — genuinely dead code, not a build target; confirmed independently by 2 same-day passes plus 2 prior audits) | `grep -rn "EnterpriseCRM" frontend/src/` = 2 matches, both inside the file itself; `ContactsV2.jsx` confirmed real via `crmApi.js`; `BusinessOS.jsx` Pipeline confirmed real and tenant-isolated via live two-org test with secret-labeled $42,000/$99,000 opportunities (see C10-026b/c below for 2 further small findings surfaced during this same investigation) | 211/211 (unaffected) | OUT OF SCOPE / ARCHIVE CANDIDATE — genuinely dead code with 2 real, working replacements already serving its function; building it would duplicate architecture, not close a gap |
| C10-027 | Named finding | HIGH | JWT logout — no server-side revocation | `_handleLogout` only cleared the cookie; token remained valid until natural expiry | **FIXED this phase** — every signed token now carries a `jti`; a small revocation ledger (`data/revoked-tokens.json`, atomic tmp-rename writes, self-pruning of expired entries) is checked in `verifyJWT`; logout AND refresh both revoke the token they're replacing | Closed | Live-verified: captured a real pre-logout token, replayed it directly after logout — correctly rejected (401, "Token invalid or expired") despite being cryptographically valid and unexpired. Same test repeated for refresh. New login after logout confirmed working with a fresh independent `jti` | 186/186 → 190/190 | FIXED |
| C10-028 | Named finding | HIGH | Sentry never wired to capture errors | `sentryService.cjs` exports functions nothing calls | **FIXED this phase (code-level).** `sentryService.captureException()` wired into 3 real error surfaces: the global Express error handler (`backend/server.js`'s catch-all `app.use((err,req,res,next)=>...)`), `process.on("uncaughtException")`, and `process.on("unhandledRejection")` — reusing the existing HTTP-envelope service exactly as-is, no new error-tracking system. Confirmed honest: `captureException()` returns `{ok:false, error:"SENTRY_DSN not set"}` without a DSN, introducing no fake success; live-verified the wiring doesn't change any existing error response shape or status code | Closed (code-level) | Live-tested: malformed-JSON 400 path unaffected (returns before reaching the new code); `node -e` direct call to `captureException()` confirmed honest `ok:false` with no `SENTRY_DSN` in `.env`; negative-tested the 2 structural regression assertions (reverted the wiring, confirmed both failed, restored) | 205/205 → 208/208 | CONFIG REQUIRED (DSN only) — the code-level portion this row originally tracked is FIXED; only the actual credential remains blocked, correctly out of this phase's authority to add |
| C10-029 | Named finding | Real, confirmed | No MRR decrement path for churn/downgrade | No churn concept existed in the deal model at all | **BUILT this phase (minimal, scoped)** — new `churnDeal()` function: a `closed_won` deal can transition to a new `"churned"` terminal stage exactly once, decrementing MRR by the exact increment formula, floor at 0, idempotent, wired through the existing workflow/route pattern | Closed | Live-verified: real deal, $120,000 value, MRR +10000 on close-won, MRR -10000 (exact restore) on churn, idempotency guard blocks double-churn and blocks re-advancing a churned deal, `dealsChurned` KPI counter increments correctly, data survived a real restart | 184/184 → 186/186 → 190/190 | FIXED |
| C10-030 | Named finding | P1/P2 (orig., partially fixed since) | Marketing external publishing: X/Twitter only | Confirmed by C.10: real, working X integration; LinkedIn/Facebook/Instagram/ads unimplemented | Not built — each additional platform requires real provider credentials to build/test meaningfully | Open | C.10's confirmation of the real X integration | N/A | CREDENTIAL BLOCKED for additional platforms — the existing X integration is real and correctly scoped; expanding requires credentials this phase must not add |
| C10-031 through C10-041 | 7 prior audit docs | Various | Various (see C.10 Discovery report for the full list) | Various | Already fixed prior to this session, re-verified live during C.10 | Closed | See `reports/C10-CROSS-SYSTEM-DISCOVERY.md` §3 for the full re-verification table | N/A | ALREADY FIXED |
| C9-PATCH | Mission brief §4.D ("First Recovery Targets") / C.9 | P0/P1 | `/coding/patch-history`, `/export`, and `/undo-patch` had zero tenant scoping — including the WRITE-side `undo-patch`, which could revert another tenant's real on-disk files by direct ID or even the platform's globally most-recent patch with no ID at all | `ai-patch-history.json` flat-array store had no `orgId` field; 3 route handlers read/wrote it unscoped | **FIXED this phase** — `orgId` recorded on every new patch (optional field, single write call site); list/export/undo all now filter or 404 on org mismatch; the no-`histId` "most recent" undo fallback is now also scoped, not just the direct-ID path | Closed | Live-verified end-to-end with two real tenants and a real applied patch: Org B's list showed empty, direct-ID export 404'd, direct-ID undo 404'd AND left the file physically untouched (re-read the file to confirm), Org A's own undo of the same patch genuinely reverted the file | 190/190 → 192/192 | FIXED |
| C10-026b (new, surfaced during Pre-Credential V1 Perfection's independent C10-026 investigation) | Pre-Credential V1 Perfection | P3 | `BusinessOS.jsx`'s real, mounted Pipeline view (`tab === "business"`) rendered `Probability: {o.probability}%` on every deal card — the real `businessDataService.cjs` opportunity record has no `probability` field, so this always rendered `undefined%`, a fabricated-looking number with no real measurement behind it | UI field referenced a backend property that was never added to the opportunity schema | **FIXED this phase** — removed the dead-field render rather than inventing a probability model; if a real probability computation is added to the backend later, it can render from that real field | Closed | Live-verified: created 2 real secret-labeled opportunities (`ORGA-SECRET-DEAL-SPHINX` $42,000 / `ORGB-SECRET-DEAL-PHOENIX` $99,000), confirmed the field was genuinely absent from the API response before removing the render; `CI=false npm run build:frontend` clean afterward | 211/211 (unaffected — frontend-only change, no backend test covers this render) | FIXED |
| C10-026c (new, surfaced during the same investigation) | Pre-Credential V1 Perfection | Informational | `RevenueOS.jsx` (1,129 lines) is a real, fully backend-connected founder revenue console (10 modules: dashboard, subscriptions, upgrade intelligence, churn, forecasting, affiliates, finance, executive, benchmark) whose only mount point is `ElectronWorkspace.jsx`, which is a documented pure passthrough in web mode — so it is unreachable on web today, exactly as `reports/OS-SALES-DISCOVERY.md` (an independent, earlier audit) already found | Its backend (`/revenue/*`) is deliberately `requireAuth + operatorOnly` gated — platform-wide founder/operator financial data, not a per-org tenant view (confirmed via direct source read of `revenueOS.js`'s router-wide gate and its own code comment explaining why) | **Not fixed — investigated and explicitly NOT recovered as a web tab.** This looks superficially identical to the `LaunchPlatform.jsx` precedent (a real component stuck behind the same Electron-only passthrough, later recovered as a normal web tab) but is a different, unsafe class of change: `LaunchPlatform`'s backend is `requireAuth`-only (safe for any regular user), while `RevenueOS`'s backend is `operatorOnly` — surfacing its nav entry to every regular tenant user would be a real information-disclosure/attack-surface increase even though the backend itself would still correctly 403 non-operators. Recovering it requires an explicit founder decision on whether/how a role-gated nav entry should exist on web (e.g., conditionally rendered only when `role==="operator"`) | N/A (no code change) | N/A | FOUNDER DECISION — do not wire without an explicit decision on role-gated web nav visibility; the underlying capability itself is real and already correctly secured server-side, so this is a reachability/UX question, not a security gap |
| 25OS-P0-1 (new, 25-OS Master Reconciliation) | 25-OS Master Reconciliation — Autonomous OS dedicated pass + this phase's own follow-up | **P0** | `/auto/v10/*` (platform-wide autonomous decision ledger + loop control, including the ability to pause the shared platform-wide autonomous loop) was gated by `requireAuth` alone — no operator check. Live-reproduced: a non-operator tenant could read the full ledger AND genuinely pause the platform's autonomous loop for every org | Missing `operatorOnly` on the `/auto` route mount, same class as the already-fixed `/eos` gap | **FIXED** — added `operatorOnly`. This phase's own follow-up investigation then found the identical pattern on `/ent`, `/eco`, `/civ` (Levels 7-9) — live-reproduced (`POST /ent/v7/companies` as a non-operator tenant returned 201, a real persisted platform-wide record) and fixed identically | Closed | Live-verified before/after on all 4 routes: non-operator tenant correctly 403's post-fix on `/auto/v10/control/mode`, `/ent/v7/companies`, `/eco/v8/tenants`, `/civ/v9/members`; negative-tested (reverted, confirmed the new regression test failed with the expected message, restored) | 211/211 → 212/212 | FIXED — the single most severe finding this entire audit arc has produced (platform-wide, write-capable, every-tenant exposure across 4 routes), fully closed |
| 25OS-P1-1 (new, 25-OS Master Reconciliation) | 25-OS Master Reconciliation — Runtime OS dedicated pass (completed by this phase after an interrupted background agent) | P1 | An agent handler reporting failure by *returning* `{success:false, error}` (not throwing) — the credential-less "ai" agent, a blocked terminal command — was not honored as a failure anywhere upstream in the dispatch chain (`agents/executor.cjs` → `executionEngine.cjs` → `autonomousLoop.cjs`), so a task/mission whose every stage genuinely failed was still stamped `"completed"` | `success: !!reply` treated a non-empty failure-sentinel string as truthy; the execution engine's main branch recorded success whenever a handler didn't throw, regardless of its own return value; the autonomous loop then stamped the overall task complete regardless | **FIXED** (found and applied by an interrupted background verification pass; independently re-verified correct and complete by this phase before certifying) — 3 coordinated minimal changes, no new architecture | Closed | Live-verified: `POST /runtime/dispatch` with a credential-less-AI-resolving command now returns `{"success":false,...}` at the top level with the real error threaded through every layer, persisted correctly to `/runtime/history`, confirmed surviving a real server restart | 212/212 (unaffected — no existing test asserted the old broken behavior) | FIXED |
| 25OS-P2-1 (new, 25-OS Master Reconciliation) | 25-OS Master Reconciliation — Agent OS dedicated pass (completed by this phase) | P2 | `agentRuntimeSupervisor.cjs`'s `_crmTick()` (the `agent_crm` autonomous agent's tick function) read `businessDataService.listLeads()`'s and `getRevenueStats()`'s return values under the wrong field names (`leads.leads` instead of `leads.items`; `revStats.totalRevenue` instead of `revStats.total`) — both always `undefined`, so the "stale lead" and "empty pipeline" autonomous health checks have never fired since I5 shipped, for any org | Field-name mismatch against `businessDataService.cjs`'s real, unchanged return shapes | **FIXED** (found and applied by an interrupted background verification pass; independently re-verified against real source by this phase before certifying) | Closed | Confirmed real field shapes via direct source read of `businessDataService.cjs` (`_list()` returns `{items,total}`; `getRevenueStats()` returns `{total,currency,count,...}`) — the fix now reads exactly those fields | 212/212 (unaffected — no existing test asserted the old broken behavior) | FIXED |
| ENGOS-1 (new, Engineering OS dedicated V1 verification — final 25-OS programme item) | Engineering OS dedicated pass | P1 | `POST /engorg/agents/:id/tick`, `/enable`, `/disable` were gated `requireAuth` only — live-reproduced a non-operator tenant (Org B) successfully disabling `engorg_backend` (one of the 20 shared, platform-wide AI engineer agents) for every user on the platform | Same control-plane class already found and fixed for `/auto/v10/control/mode`, `/ent`, `/eco`, `/civ` — a write action with no legitimate ordinary-user use case, disrupting shared platform state for every tenant | **FIXED** — added `operatorOnly` to all 3 routes. Legitimate ordinary-user actions (creating/claiming work items, creating objectives/epics) deliberately left `requireAuth`-only, matching the precedent already established for `/bizorg` and `/missions` | Closed | Live-verified before/after on a fresh server process: non-operator `disable`/`enable`/`tick` all correctly 403 post-fix; ordinary-user `POST /engorg/v2/work-items` confirmed unaffected (still 200); negative-tested (reverted, confirmed the new regression test failed with the expected message, restored) | 212/212 → 214/214 | FIXED |

---

## Summary counts

_Updated in MASTER FINAL GAP CLOSURE (2026-08-15). A concurrent same-day pass (`reports/FINAL-V1-GAP-CLOSURE.md`, "Pre-Credential Gate") reconciled the same remaining items via re-classification only, with no code changes. This pass went further on 3 items that pass explicitly left open (C10-007's `event` type, C10-012, C10-026) — see `reports/MASTER-FINAL-CLOSURE.md` for the full reconciliation against that report's claims. All counts below reflect the current, most complete state across all 43 distinct IDs (41 C10 + C9-PATCH + C10-017b)._

| Disposition | Count |
|---|---:|
| FIXED / BUILT (across Master Recovery + Master Residual Closure + this Final Gap Closure) | 11 (C10-003, C10-004, C10-007‡ [event type], C10-008, C10-009, C10-012, C10-017, C10-027, C10-028‡ [code-level], C10-029, C9-PATCH) |
| ALREADY FIXED (confirmed, no action needed — includes C10-007's manual+schedule triggers) | 14 (C10-001, C10-002, C10-007*, C10-013, C10-031 through C10-041) |
| OUT OF SCOPE / ARCHIVE CANDIDATE (genuinely dead code, real replacement exists — building would duplicate architecture) | 1 (C10-026, reclassified this phase from "BUILD REQUIRED") |
| CREDENTIAL BLOCKED for further platform expansion (base capability real) | 1 (C10-016, C10-030) |
| VERIFY / FOUNDER DECISION (needs a product/architecture decision before any fix is meaningful; canonicality documented in MASTER-RESIDUAL-CLOSURE-PLAN.md) | 6 (C10-005, C10-004b, C10-006, C10-010, C10-024, C10-017b) |
| POST-V1 / FOUNDER DECISION (threshold/webhook/approval trigger semantics only — event type now closed) | 1 (C10-007's remaining 3 non-dispatched trigger types†) |
| CONFIG REQUIRED | 1 (C10-028, DSN credential only — code wiring itself is now FIXED) |
| DEFERRED (no evidence of urgency, no security/tenant dimension) | 6 (C10-011, C10-015, C10-018, C10-022, C10-023, C10-025) |
| OUT OF SCOPE (intentional/architectural, not a bug) | 4 (C10-014, C10-019, C10-020, C10-021) |
| FALSE POSITIVE | 0 |

*Note: C10-004 fix also closes the C.9-tracked mission-context leak, counted once here under its C.10 ID.*
*C10-016 and C10-030 remain credential-blocked for expanding beyond their existing real, working base capability (connectors / X-integration respectively) — not counted as a separate "BUILD REQUIRED" bucket since no unilateral build is possible without credentials.*
*C10-017b is a new ID surfaced during the C10-017 audit (external-ingestion orgId gap) — distinct root cause from C10-017 proper, which is fully closed.*
*C10-007† — manual+schedule triggers (real, working, pre-existing) = ALREADY FIXED. C10-007‡ — the `event` trigger type was genuinely built and live-verified THIS phase (a real concurrency bug was found and fixed along the way). Only `threshold`/`webhook`/`approval` (3 of the original 4 non-dispatched types) remain a named, explicit POST-V1/FOUNDER DECISION gap — not silently unresolved.*
*C10-008‡ — `deleteRule` + manual `/fire` route BUILT this phase (the concurrent Pre-Credential Gate pass had left this at "not fixed, POST-V1, P2, small").*
*C10-012‡ — Support OS frontend BUILT this phase, live two-tenant verified (the concurrent Pre-Credential Gate pass had left this at "POST-V1, honesty already satisfied via disclosure" — that disclosure-based reasoning remains valid but is now moot since the real backend is wired).*
*C10-028‡ — Sentry code-level wiring BUILT this phase (the concurrent Pre-Credential Gate pass had left this at "not fixed this phase, not named in this gate's scope"). Only the DSN credential itself remains blocked.*

**Remaining P0: 0. Remaining exploitable P1 (security/tenant/data-integrity): 0. Remaining P1 non-security: 0 — C10-007 reclassified this phase (manual+schedule work; the remaining 4 trigger types are a founder/product decision, not a functionality defect). Open VERIFY items (C10-004b/C10-005/C10-006/C10-010/C10-024) all require a product decision, none with a live-reproduced leak. Every remaining P2/P3 item has an explicit, evidenced disposition — none left as vague "open."**

**Total distinct IDs: 41 (C10-001 through C10-041), plus C9-PATCH (named in the mission brief's own "First Recovery Targets"), plus C10-017b (newly surfaced this phase) = 43 total. Every one has an explicit disposition above. Full per-item Pre-Credential Gate reconciliation: `reports/FINAL-V1-GAP-CLOSURE.md`.**
