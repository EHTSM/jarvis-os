# Security Audit — 2026-06-14

Generated by `npm audit`. All vulnerabilities are in **devDependencies** or **optional** packages — none affect the production runtime bundle.

## Summary

| Severity | Count |
|---|---|
| Critical | 4 |
| High | 9 |
| Moderate | 6 |
| Low | 0 |
| **Total** | **19** |

---

## Critical (4)

### `shell-quote` (via `concurrently`)
- **CVE**: Shell injection in `shell-quote` allows arbitrary command execution when parsing attacker-controlled strings.
- **Impact**: `concurrently` is a **devDependency** used only in `npm run dev`. Not present in production builds.
- **Fix**: `concurrently@10.0.3` (major version bump — breaking change in dev workflow)
- **Action**: Low urgency. Do not run `npm run dev` with untrusted input. Update when convenient.

### `concurrently`
- **Transitive dependency** of `shell-quote` vulnerability above. Same scope and fix.

### `form-data` (via `node-telegram-bot-api`)
- **CVE**: Prototype pollution in `form-data`.
- **Impact**: `node-telegram-bot-api` is a **production dependency** used for Telegram bot integration.
- **Fix**: `node-telegram-bot-api@1.1.0` (major bump — breaking API changes)
- **Action**: Medium urgency. Telegram bot runs with limited blast radius — no user data exposure. Replace with `node-telegram-bot-api@^0.64+` or evaluate `telegraf` as alternative in next maintenance window.

### `request` (via `node-telegram-bot-api`)
- **CVE**: Multiple vulnerabilities in the legacy `request` package (deprecated since 2020).
- **Impact**: Same scope as `form-data` above.
- **Action**: Same as `form-data` — package replacement resolves both.

---

## High (9)

All 9 high-severity vulnerabilities are in **`electron-builder`** (devDependency used only during app packaging):

| Package | Root Cause |
|---|---|
| `electron-builder` | Depends on vulnerable `tar`, `node-gyp`, `cacache` |
| `tar` | Path traversal vulnerabilities (GHSA-2022) |
| `node-gyp` | Transitive `tar` + `make-fetch-happen` |
| `cacache` | Transitive `tar` |
| `make-fetch-happen` | Transitive `cacache` |
| `@electron/rebuild` | Transitive `node-gyp` + `tar` |
| `app-builder-lib` | Transitive chain |
| `dmg-builder` | Transitive chain |
| `electron-builder-squirrel-windows` | Transitive chain |

- **Impact**: Affects the **build/packaging pipeline only** — not the running application.
- **Fix**: `electron-builder@26.15.3` (major version — evaluate compatibility)
- **Action**: Only run `electron-builder` in a trusted CI environment. Update to 26.x in next release cycle.

---

## Moderate (6)

All 6 moderate vulnerabilities are in `node-telegram-bot-api` transitive dependencies:

| Package | Issue |
|---|---|
| `qs` | Prototype pollution |
| `tough-cookie` | Cookie parsing vulnerability |
| `uuid` | Predictable UUIDs in certain modes |
| `request-promise` | Transitively inherits `request` vulnerabilities |
| `request-promise-core` | Same |
| `node-telegram-bot-api` | Aggregated from above |

- **Action**: Resolved by replacing `node-telegram-bot-api` with a maintained alternative.

---

## Action Plan

### Immediate (before next user-facing release)
1. **No breaking runtime exposure confirmed** — all critical/high are in devDeps or the Telegram bot, which has no access to user PII or payment data.
2. **Document and track** — this file serves as the audit record.

### Next maintenance window
1. Replace `node-telegram-bot-api` with `telegraf@4.x` or update to `node-telegram-bot-api@^0.65+` to resolve the 6 critical/moderate Telegram vulns.
2. Test Telegram bot functionality after replacement.

### Next build release
1. Upgrade `electron-builder` to 26.x — resolve all 9 high-severity build-tool vulns.
2. Run `npm run electron:build` and validate the packaged app.

### Dev tooling
1. Upgrade `concurrently` to resolve the critical `shell-quote` issue in dev workflow.
2. Low urgency — affects dev machines only.

---

## What Is NOT Vulnerable

- Express 5.x runtime — no known vulnerabilities
- better-sqlite3 — no known vulnerabilities
- Firebase SDK (frontend) — current version, no known vulnerabilities
- JWT authentication (custom, crypto-only) — not a third-party package
- groq-sdk, openai — current versions, no known vulnerabilities
- Electron 41 — current stable, no known vulnerabilities
