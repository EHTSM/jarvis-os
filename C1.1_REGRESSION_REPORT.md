# C.1.1 REGRESSION REPORT

Date: 2026-08-13
**PASS / FAIL / BLOCKED are reported separately. Blocked suites are NOT counted as passed.**

---

## Runtime suite — the gate

```
npm run test:runtime
```

| Run | Result |
|---|---|
| C.1.1 baseline (before any change) | **144/144 pass, 0 fail** |
| C.1.1 final (after all fixes) | **144/144 pass, 0 fail** |

50 suites, 144 tests, ~6.9 s. **No regression introduced by this phase.**

---

## Security suites — 94 total

| Class | Count |
|---|---:|
| **PASS** | **59** |
| **FAIL** | **1** |
| **BLOCKED** (prerequisite not met) | **34** |

### The single genuine FAIL — pre-existing

**`19-logging-consistency.cjs`**
```
✗ zero unallowlisted files use raw console.* in backend/routes or backend/services
  — found: backend/services/companyWorkspaceBuilder.cjs
```

**Classified PRE-EXISTING.** `companyWorkspaceBuilder.cjs` was **not touched by this phase** (`git status` on that path: clean). Per instruction, unrelated logging debt was **not** modified. This failure is carried forward unchanged and is not counted as a pass.

### Initial classification was wrong — corrected

An automated sweep first reported **8 failures**. Retesting each serially showed **7 were rate-limit prerequisites, not failures**:

| Suite | First result | On retry |
|---|---|---|
| `43-growth-os-tenant-isolation` | FAIL | **PASS — 13/13** |
| `44-content-seo-tenant-isolation` | FAIL | **PASS — 13/13** |
| `45-distribution-tenant-isolation` | FAIL | BLOCKED — signup rate-limited |
| `36-org-network-ai-ecosystem-idor` | FAIL | BLOCKED — signup rate-limited |
| `39-company-dashboard-org-scoping` | FAIL | BLOCKED — signup rate-limited |
| `28-product-truth-consistency` | FAIL | BLOCKED |
| `57-reports-page-shows-fake-zero-stats` | FAIL | BLOCKED |

All five blocked suites fail at the same precondition: *"a real authenticated session was obtained — signup/login failed"*, after `registration rate-limited, waiting 30s (attempt 5/5)`. **The rate limiter was triggered by this phase's own probe accounts.** That is a test-environment condition, not a product defect — and reporting them as failures would have been wrong.

### BLOCKED breakdown (34)

| Reason | Count |
|---|---:|
| Requires a frontend dev server on `:3000` (not running) | 25 |
| Signup/login rate-limited by this phase's probing | 8 |
| Other timeout | 1 |

**These were not run. They are not passes.**

---

## New regression tests — 22 assertions, all passing

Added only for defects genuinely found and fixed. No test manufactured to inflate counts.

### `tests/security/91-api-404-boundary.cjs` — 5 assertions
```
✓  6 unknown API paths each return JSON 404
✓  EnterpriseOS.jsx's endpoints report themselves as missing, not as 200
✓  3 live API routes still return 200 JSON
✓  auth gate still returns 401 JSON without a session
✓  GET / still serves the SPA shell (fallback intact)
```
Fails loudly if the catch-all ever masks an unknown API route again.

### `tests/security/92-c11-runtime-defect-regressions.cjs` — 9 assertions
```
D1  ✓  foreign operator record no longer throws (was: 500 on every call)
D1  ✓  mixed store returns only real account records, with correct pct
D1  ✓  empty store and 5 malformed value shapes handled without throwing
D1  ✓  getProgress() and completeStep() carry the same shape guard
D2  ✓  getStats() returned 5 recentRuns, all commands normalised
D2  ✓  getDashboard() returns ok:true instead of 500
D3  ✓  scan completed in 2583ms (was 25,000-35,000ms)
D3  ✓  deterministic: 3585 smells, 693 dead_export, both runs
D3  ✓  per-scan file memo released between calls (2294 files each run)
```
D3 asserts **both** speed and output-equivalence — an optimisation that changed results would fail.

### `tests/security/90-phase-c1-search-alias-coverage.cjs` — 8 assertions (from C.1)
```
✓  82/82 surfaces aliased    ✓  all 82 labels resolve
✓  16 founder-vocabulary probes resolve to their real surface
✓  AI & Agents 11/11 · Intelligence 16/16 · Org Levels 6/6
```

### Related pre-existing suites — still green
```
75-benchmark-search-vocabulary-gaps      9/9
50-developer-search-terminology-gaps    26/26
43-growth-os-tenant-isolation           13/13
44-content-seo-tenant-isolation         13/13
```

---

## Tenant isolation — directly re-verified after the routing change

The API 404 boundary alters request routing, so isolation was re-tested from scratch:

```
A → B's org, 12 endpoints:  403 × 12  (0 leaks)
B → B's own org, control :  200 × 2   (owner access preserved)
VERDICT: tenant isolation intact ✓
```

Covers `/orgs/*`, `/org-ai`, `/org-agents`, `/org-automation`, `/org-graph`, `/org-workspace`, `/org-executive`, `/enterprise/audit`, `/enterprise/dashboard`.

---

## Honest summary

| Category | Count | Note |
|---|---:|---|
| Runtime tests passing | 144/144 | gate, green before and after |
| Security suites passing | 59 | |
| Security suites failing | **1** | pre-existing, untouched, carried forward |
| Security suites blocked | **34** | not run, **not counted as passed** |
| New assertions added | 22 | all for real defects found in this phase |

**No failure was hidden. No blocked suite was counted as a pass. No unrelated debt was modified to make a suite green.**
