# A.11.2 — Regression Report

**Date:** 2026-08-13 · **Branch:** `security/reality-completion`
**Commits:** `46946cc1` (F3) · `e90ddf44` (mobile/table)
**No merge. No push. `.env` unmodified.**

---

## 1. Runtime

```
npm run test:runtime
ℹ tests 144   ℹ pass 144   ℹ fail 0
```

## 2. Suites

| Suite | Result | Note |
|---|---|---|
| `25-accessibility-contrast` | **9 / 9** | unchanged |
| `26-accessibility-foundation` | **21 / 22** | **held** — the single failure is the B19.4 form-labelling gap `G1-B193`, unchanged and NOT converted to a pass |
| `27-visual-accessibility` | **16 / 16** | unchanged |
| `28-keyboard-aria-recovery` | **14 / 14** | unchanged |
| `29-a11-ux-consistency` | **12 / 12** | 9 → 12 (F3 coverage added) |

## 3. Build

```
Compiled successfully.
```
Zero warnings.

## 4. Test 89 — byte-identical

```
$ shasum -a 256 -c /tmp/a112-test89.sha
tests/security/89-cross-product-ux-consistency-sweep.cjs: OK
```

SHA-256 captured at phase start and re-verified at phase end:
`aa5b00db…9bfebcb`. **Not modified.**

---

## 5. New regression coverage (suite 29: 9 → 12)

| Assertion | Guards |
|---|---|
| no user-initiated mutation swallows its API error | F3 — all 15 recovered paths |
| local `API()` helpers check response status | the 4 helpers that returned `r.json()` unchecked |
| NEGATIVE: a silent mutation catch is detectable | prevents the guard rotting into a tautology |

## 6. Live re-verification

| Item | Before | After |
|---|---|---|
| F3 category-C silent mutations | 15 | **0** |
| `.aud-table` @390 right edge | 486 (outside) | **374 (inside)** |
| `.aud-table` overflow-x | `visible` | **`auto`** |
| Cross-surface walk (25 surfaces) | — | **0 missing headers, 0 h-scroll, 1 page error** (credential 403) |

## 7. Prior recoveries — all intact

| Recovery | Check | Result |
|---|---|---|
| More-menu aliases | entries with `alias` | **76 / 76** |
| ⌘K NAV_ACTIONS contract | alias→keyword drift | **0** (suite 29) |
| Mobile topbar | media block present | ✓ |
| ctx-sidebar theme | `var(--bg)` | ✓ |
| Agent Registry | `|| []).map` guards | **4** |
| AgentOSV2 drawer | `useEscapeKey(!!drawer` | ✓ |
| End of Day Review | `Array.isArray` guards | **4** |
| Global Activity header | `gaf-header` | ✓ |

**No regression introduced by this phase.**

## 8. Reverted work

Three R2 attempts (`min-width: 0` on `.cmd-quick-actions` children, on
`.cmd-layout`, on `.cmd-root`) did not move the measurement and were **reverted**
rather than left as dead CSS asserting a fix that did not happen.

## 9. Test integrity

No test weakened, skipped or relaxed. One test file extended (+3 assertions).
Suite 26's failing assertion preserved exactly.
