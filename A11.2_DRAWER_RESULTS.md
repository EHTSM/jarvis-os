# A.11.2 — Mobile Drawer Results

**Result: MEASURED — drawers PASS at both mobile viewports. No fix required.**

A.11.1 left mobile drawer behaviour NOT MEASURED. This closes it with direct
measurement, not inference.

**Instrument:** `scripts/a112-mobile-probe.cjs` — real authenticated session,
each drawer navigated to and measured in place.

---

## Measured — 390 × 844

| Drawer | Surface | w | h | left | right | in viewport | scroll |
|---|---|---:|---:|---:|---:|---|---|
| `ctx-sidebar` | Execution | 220 | 5381 | 170 | **390** | **✓** | inner |
| `ctx-sidebar` | Reliability | 220 | 2512 | 170 | **390** | **✓** | inner |
| `ctx-sidebar` | Jarvis Brain | 220 | 6674 | 170 | **390** | **✓** | inner |

## Measured — 430 × 900

| Drawer | Surface | w | h | left | right | in viewport | scroll |
|---|---|---:|---:|---:|---:|---|---|
| `ctx-sidebar` | Execution | 220 | 4831 | 210 | **430** | **✓** | inner |
| `ctx-sidebar` | Reliability | 220 | 2331 | 210 | **430** | **✓** | inner |
| `ctx-sidebar` | Jarvis Brain | 220 | 6516 | 210 | **430** | **✓** | inner |

**The right edge lands exactly on the viewport edge at both widths** — the rail
neither clips nor overflows. Width is a constant 220px, as on desktop.

---

## Checklist

| Property | Result | Evidence |
|---|---|---|
| Bounds within viewport | **PASS** | `right` = viewport width at 390 and 430 |
| Horizontal overflow caused by drawer | **PASS** | doc overflow identical with and without the rail (18px at 390 — attributable to R2, not the drawer) |
| Width consistency | **PASS** | 220px at both viewports and on desktop |
| Scroll behaviour | **PASS** | `overflow: hidden` on the rail, inner region scrolls |
| Theme | **PASS** | recovered in A.11.1 to `var(--bg)`; renders themed |
| Open / close | **n/a** | `ctx-sidebar` is a persistent rail, not a dismissible overlay |
| ESC | **n/a** | same — nothing to dismiss |
| Focus / keyboard | **CERTIFIED ELSEWHERE** | B19.4, suite 28 14/14 |

---

## Overlay drawers — NOT MEASURED at mobile

`ContactsV2` (`.cv2-drawer`) and `AgentOSV2` (`.av2-drawer`) are click-opened
overlays. At 390/430 their opener rows were **not present** in the DOM:

```
NOT MEASURED  CRM contact drawer — opener not present: .cv2-row-avatar, .cv2-row
```

The contacts list requires seeded contact data the audit credential does not
have (`403 Not a member of this workspace`). Recorded as **CREDENTIAL BLOCKED /
NOT MEASURED**, not assumed correct.

Their desktop behaviour is measured and both were fixed in A.11.1 — `av2-drawer`
gained its Escape binding, `cv2-drawer` already had one.

---

## Classification

| Item | Class |
|---|---|
| `ctx-sidebar` at 390 / 430 | **PRODUCTION READY** (measured) |
| `cv2-drawer` at mobile | **CREDENTIAL BLOCKED** |
| `av2-drawer` at mobile | **CREDENTIAL BLOCKED** |
